package Unidades;
/*
import Edificios.Cuartel;
import Tablero.ElementoFueraDeRangoException;
import Tablero.Mapa;
import Tablero.Posicion;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertThrows;
import static org.junit.jupiter.api.Assertions.assertTrue;
*/
public class AldeanoTest {
/*    Mapa mapa = new Mapa();
    Posicion p = new Posicion(mapa,0,0);

    @Test
    void creoAldeano(){
        Aldeano A1 = new Aldeano(p);
        assertTrue(A1 != null);
    }
/*
    @Test
    void repararEdificioNoContiguoLevantaExcepcion(){
        Mapa mapa = new Mapa();
        Posicion p = new Posicion(mapa,0,0);
        Aldeano aldeano = new Aldeano(p);
        mapa.agregarElementoDeMapa(aldeano,p, 1,1);
        Cuartel cuartel = new Cuartel();
        mapa.agregarElementoDeMapa(cuartel,p, 5,5);

        assertThrows(ElementoFueraDeRangoException.class,()->{
            aldeano.repararEdificio(cuartel);
        });
    }*/
}